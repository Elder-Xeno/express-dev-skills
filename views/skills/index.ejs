<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Developer Skills</title>
  </head>
  <body>
    <h1>My Developer Skills</h1>
    <ul>
      <% skills.forEach(skill => { %>
        <li>
          <a href="/skills/<%= skill.id %>"><%= skill.name %></a> - <%= skill.status %>
        </li>
      <% }); %>
    </ul>
  </body>
</html>